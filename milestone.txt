Next Step
-------------------

We have successfully updated main_train.py (and its dependencies) to handle validation loops and plotting, which covers the first major part of the mandatory requirements.

The next mandatory requirement is to update predict.py. According to the subject PDF, the prediction program must:

    Load the weights.

    Perform prediction.

    Evaluate it using the binary cross-entropy error function: